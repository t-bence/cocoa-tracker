[project]
name = "cocoa-tracker"
version = "0.2.0"
description = "Scraper for Budapest Festival Orchestra Cocoa Concerts with Telegram notifications"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "requests>=2.32.5",
    "python-dotenv>=1.0.1",
    "pip>=26.0.1",
    "boto3>=1.40.30",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "google-api-python-client>=2.162.0",
    "google-auth>=2.38.0",
    "openai>=1.50.0",
]

[dependency-groups]
dev = [
    "boto3>=1.40.30",
    "commitizen>=4.9.1",
    "mypy>=1.11.2",
    "pre-commit>=4.3.0",
    "pytest>=8.4.2",
    "ruff>=0.9.0",
    "types-requests>=2.32.0",
    "pre-commit>=4.5.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "$version"
version_scheme = "pep440"
version_provider = "uv"
update_changelog_on_bump = true
major_version_zero = true
